extends layout

block content
	div.container
		div.row
			div.col-sm-6.col-md-4.col-md-offset-4
				#user
					h1.text-center.login-title Bem-vindo #{user.firstName}. 
					h1.text-center.login-title Verifique seus dados abaixo:
						div.signup-wall
							ul.user-details
								li Usuário => #{user.username}
								li E-mail    => #{user.email}
								li Nome => #{user.firstName} 
								li Sobrenome => #{user.lastName}
						a(href='/signout', class='text-center new-account') Sair